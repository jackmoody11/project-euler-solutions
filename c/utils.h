#ifndef UTILS_H
#define UTILS_H

int is_prime(long n);          // Test if int is prime
long max_prime_factor(long n); // Find max prime factor
long sum_to(long n);           // Sum of natural numbers to n
#endif                         /* UTILS_H */