#ifndef UTILS_H
#define UTILS_H

int is_prime(long n);          // Test if int is prime
long max_prime_factor(long n); // Find max prime factor
int is_palindrome(long n);     // Test if number is palindrome
#endif                         /* UTILS_H */